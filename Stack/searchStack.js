const deepCopy = require('../optimisation/deepCopy')

/**
 * first of all i make a deepCopy of the given Stack
 * @param {Stack data type} stack 
 * @param {any} value 
 */

function searchInStack(stack, value) {

  const deepStackCopy = deepCopy(stack.storage)

}

module.exports = searchInStack